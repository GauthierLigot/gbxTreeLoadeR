% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_saveddb.R
\name{get_saveddb}
\alias{get_saveddb}
\title{Load the Most Recent Saved Database Snapshot}
\usage{
get_saveddb(dir)
}
\arguments{
\item{dir}{Character path to an existing directory containing the saved
database file(s).}
}
\value{
The \code{importedDB} object (a named list of tables) loaded from the
most recent file. Returns \code{NULL} (invisibly) if the directory is missing
or invalid.
}
\description{
Scans a directory for files and loads the most recent file (by filename sort),
assuming it is an \code{.RData} file created by \code{get_db(save = TRUE, ...)}
that stores an object named \code{importedDB}. Returns that object.
}
\details{
\itemize{
\item This function expects the \code{.RData} file to contain an object named
\code{importedDB}, as produced by \code{get_db(save = TRUE, ...)}.
\item The "most recent" file is chosen by sorting the file names in \strong{descending}
order; this assumes your filenames include a sortable timestamp (e.g.,
\code{Rdatabase_YYYYmmdd_HHMMSS.RData}).
\item No filtering by extension is performed in this basic version.
}
}
\examples{
\dontrun{
db <- get_saveddb("/path/to/snapshots")
names(db)
}

}
