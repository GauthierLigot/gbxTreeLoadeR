% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_table.R
\name{get_table}
\alias{get_table}
\title{Retrieve a Table from the DendroPulse Database}
\usage{
get_table(con, tablename = "tree")
}
\arguments{
\item{con}{A live database connection created with
\code{get_dendropulse()} or any valid \code{DBIConnection}.}

\item{tablename}{Character string. Name of the table to retrieve.
Defaults to \code{"tree"}.}
}
\value{
A data.frame or \code{sf} object containing the table contents.
Returns \code{NA} if the table does not exist.
}
\description{
This function retrieves a complete table from the connected PostgreSQL
database. If the table name is \code{"xy"}, the function reads it as a spatial
table using \code{sf::st_read()}. For all other tables, a standard SQL
\verb{SELECT *} query is executed using \code{DBI::dbGetQuery()}.
}
\details{
\itemize{
\item Checks if the table exists using \code{getTableList()}.
\item Applies a simple date conversion if a column named \code{"date"} exists.
\item Table \code{"xy"} is assumed to be spatial and is read using \code{sf::st_read()}.
}
}
\examples{
\dontrun{
con <- get_dendropulse()
df <- get_table(con, "tree")
head(df)

xy <- get_table(con, "xy")  # returned as sf object
}

}
